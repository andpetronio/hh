---
import Layout from "../../layouts/Layout.astro";
import ProfileForm from "../../components/maners/ProfileForm.astro";
import { resolveRequestLocale } from "../../i18n/resolveRequestLocale";
import { getTranslations } from "../../i18n";

// 🌍 idioma
const locale = resolveRequestLocale(Astro.request);
const t = getTranslations(locale);
---

<Layout
  title="Regístrate como Maner"
  description="Únete a Manify y conectá con clientes que necesitan tus manos hábiles"
  locale={locale}
  t={t}
>
  <section class="max-w-3xl mx-auto px-4 py-10">
    <header class="mb-10">
      <h1 class="text-3xl sm:text-4xl font-semibold text-gray-900 mb-3">
        {t.header.becomeManer}
      </h1>
      <p class="text-gray-600">
        {t.createManers.strings.subtitle}
      </p>
    </header>

    <ProfileForm t={t} />
  </section>
</Layout>
