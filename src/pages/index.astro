---
import Layout from "../layouts/Layout.astro";
// import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import TradeGrid from "../components/TradeGrid.astro";
import AdBanner from "../components/AdBanner.astro";
import Stats from "../components/Stats.astro";
import HowItWorks from "../components/HowItWorks.astro";

import { getTranslations } from "../i18n";
import { resolveRequestLocale } from "../i18n/resolveRequestLocale";
// 🌍 idioma
const locale = resolveRequestLocale(Astro.request);
const t = getTranslations(locale);
---

<Layout
  title="Manify - Encuentra Maners para tu hogar"
  description="Conecta con Maners verificados en plomería, jardinería, cuidado infantil y más"
  locale={locale}
>
  <main class="min-h-screen">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 py-8 sm:py-16 space-y-20">
      <!-- Hero -->
      <Hero t={t} />

      <!-- Categorías -->
      <TradeGrid t={t} />

      <!-- Banner publicitario -->
      <AdBanner t={t} />

      <!-- Estadísticas -->
      <Stats t={t} />

      <!-- Cómo funciona -->
      <HowItWorks t={t} />
    </div>
  </main>
</Layout>
